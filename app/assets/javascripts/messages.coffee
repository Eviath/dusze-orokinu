jQuery ->
  $('.chosen-it').chosen({no_results_text: "Nie znaleziono", placeholder_text_multiple:'Wybierz odbiorców wiadomości.'}); 
  
  elem = document.getElementById('messages');
  if !elem

  else
    elem.scrollTop = elem.scrollHeight
