.login-page-header

  .login-page-bg
  .login-page-wrapper

    .login-page-content

      .page-header
        - provide(:title, "Logowanie")
        %h1 Logowanie
        %h3 Zaloguj się, aby zyskać dostęp do wszystkich funkcji strony.


      .container.form-container
        .row.justify-content-md-center
          .form-group.col-md-6
            = form_for(:session, url: login_path) do |f| 
              = f.email_field :email, placeholder: "E-mail", class: 'form-control' 
              = f.password_field :password, placeholder: "Hasło", class: 'form-control' 
              %p
                = link_to "(Zapomniałem/am hasła)", new_password_reset_path

              = f.submit "Zaloguj", class: "btn user-btn" 
              
              = f.label :remember_me, class: "checkbox inline" do
                = f.check_box :remember_me
                Zapamiętaj mnie.

              %p 
                Nie masz konta?
                = link_to "Zarejestruj się!", register_path 
