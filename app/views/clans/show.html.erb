<% provide(:title, 'Klan - Show') %>

<div class="clan_show_content">

<div class="container">



  <div class="alliancerequest">
    <%= render @clan %>
  </div>

  <div class="alliancerequest-button text-center">
    <% if @clans.previous(@clan)%>
    <%= link_to 'Poprzedni', @clans.previous(@clan), class: "btn btn-do" %>
    <% end %>

  <%= link_to "Powrót - Lista klanów", clans_path, class: "btn btn-do" %>

  <% if @clans.next(@clan)%>
  <%= link_to 'Następny', @clans.next(@clan), class: "btn btn-do" %>
  <% end %>

  </div>




</div>
</div>
