
.login-page-header

  .login-page-bg
  .login-page-wrapper

    .login-page-content

      .page-header
        - provide(:title, "Przypomnij hasło")
        %h1 Zapomniałeś/aś hasła?
        %h3 Podaj swoje dane i podążaj za instrukcjami w wiadomości e-mail.

      .container.form-container
        .row.justify-content-md-center
          .form-group.col-md-6

            = form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f|
              = devise_error_messages!

              .field
                = f.label :email, 'Adres E-mail'
                = f.email_field :email, autofocus: true, autocomplete: "email", class: 'form-control'


              .actions
                = f.submit "Wyślij mi instrukcję resetu hasła", class: 'btn btn-do-main'


            = render "devise/shared/links"
